---
const { href } = Astro.props;
---

<!-- react is better than this. how are you worse than react? -->
<!-- just put the whitespace in the bag -->
<a href={href} class="underline text-blue-700"><slot /></a>